The Backend

- This is where the server resides. It handles requests from users on the app,
and perfoms CRUD operations on the database.
- app.js is the file that handles the requests
- package.json is a module manager for modules used by the backend to be able to 
perfom it's functions
- node_modules folder holds all the modules that have been installed as listed on
package.json
