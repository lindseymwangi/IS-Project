The Store Folder

- This is a volatile storage system that is used by the app to hold temporary
data.
- It stores messages thrown by the APIs to render them on other pages using modals
- It also stores the navigation state, so the user can know where the are on the
app
