The Src folder

- This folder holds all the app files, everything that is in the app, resides
here
- It contains images used by the app, stored in the src/assets folder
- It contains code used on the App
- It contains a volatile app storage to help the app perfom it's functions
- Contains code that communicates with the backend by sending CRUD requests
