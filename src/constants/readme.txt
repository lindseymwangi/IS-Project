The Constants Folder

- This folder store files that are constant(they don't change).
- It holds files that store information on colors to use on the app, as well as
styles that are shared across the app.
